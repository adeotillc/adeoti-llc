server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # This is the magic line that fixes React Router 404 errors!
    location / {
        try_files $uri $uri/ /index.html;
    }
}